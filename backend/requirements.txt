# Phase 2 backend — local proof of concept
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
numpy>=1.26
pillow>=10.2
httpx>=0.27          # used by fastapi TestClient (smoke_test.py)

# Optional (real reconstruction). The pipeline falls back to a mock-pose path
# when these / the COLMAP binary are not present, so the POC runs without them.
#   pip install pycolmap opencv-python-headless
# COLMAP binary must be installed separately:  https://colmap.github.io/install.html
#   macOS:  brew install colmap
#   ubuntu: apt install colmap
